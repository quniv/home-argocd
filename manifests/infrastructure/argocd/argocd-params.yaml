---
apiVersion: v1
kind: ConfigMap
metadata:
  name: argocd-cmd-params-cm
  namespace: argocd
data:
  # TLS is terminated by the Cilium Gateway — ArgoCD runs plain HTTP internally.
  server.insecure: 'true'
