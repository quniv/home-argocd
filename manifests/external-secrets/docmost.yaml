---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: docmost-creds
  namespace: docmost
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: infisical
    kind: ClusterSecretStore
  target:
    name: docmost-creds
    creationPolicy: Owner
    deletionPolicy: Retain
  data:
    - secretKey: APP_SECRET  # checkov:skip=CKV_SECRET_6:False positive — Infisical path reference, not an encoded secret value
      remoteRef:
        key: /docmost/APP_SECRET
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: docmost-psql
  namespace: docmost
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: infisical
    kind: ClusterSecretStore
  target:
    name: docmost-psql
    creationPolicy: Owner
    deletionPolicy: Retain
  data:
    - secretKey: user-password  # checkov:skip=CKV_SECRET_6:False positive — Infisical path reference, not an encoded secret value
      remoteRef:
        key: /docmost/POSTGRES_PASSWORD
    - secretKey: postgres-password  # checkov:skip=CKV_SECRET_6:False positive — Infisical path reference, not an encoded secret value
      remoteRef:
        key: /docmost/POSTGRES_ADMIN_PASSWORD
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: docmost-redis
  namespace: docmost
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: infisical
    kind: ClusterSecretStore
  target:
    name: docmost-redis
    creationPolicy: Owner
    deletionPolicy: Retain
  data:
    - secretKey: redis-password  # checkov:skip=CKV_SECRET_6:False positive — Infisical path reference, not an encoded secret value
      remoteRef:
        key: /docmost/REDIS_PASSWORD
