---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: tls-cert
spec:
  secretName: tls-cert
  usages: [digital signature, key encipherment]
  dnsNames: ['*.qtlab.dev', '*.multica.qtlab.dev']
  issuerRef:
    name: letsencrypt
    kind: ClusterIssuer
